# Agent Instructions

The canonical agent-facing memory for this repo is [`CLAUDE.md`](CLAUDE.md).
Read that file — do not duplicate its content here.

Quick orientation:

- Static SPA in `index.html` + committed prerendered route files generated by
  `scripts/prerender.py` (also the Cloudflare Pages build command).
- All three forms POST to n8n webhooks at `voidnox.app.n8n.cloud`
  (Make.com and Tally are decommissioned).
- Deploy = fast-forward push to `main` over SSH.
- Run `python3 scripts/check-trademark-risk.py` before pushing copy changes.
- July 2026 revamp record: `docs/WEBSITE-REVAMP-2026-07.md`.
  Environment/DNS/email: `docs/ENVIRONMENT.md`.
- `INFRASTRUCTURE.md` holds live secrets and must stay untracked.
